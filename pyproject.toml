[project]
name = "find-security-news"
version = "0.1.0"
description = "Security news collection and AI summarization workflow"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31",
]

[tool.ruff]
line-length = 100
